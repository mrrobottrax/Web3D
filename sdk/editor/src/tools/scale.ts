import { Viewport } from "../windows/viewport.js";
import { SelectExtension } from "./selectextension.js";

export class ScaleTool extends SelectExtension {
	draw(viewport: Viewport) {
		super.draw(viewport);
	}
}